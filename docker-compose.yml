version: '3.8'

services:
  hono-test:
    build: .
    ports:
      - "3334:3334"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3334/" ]
      interval: 30s
      timeout: 5s
      retries: 3
